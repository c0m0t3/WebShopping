export interface ShoppingList {
  id: number;
  name: string;
  description: string;
  store: string;
} //TODO ka ob man das hier macht

export interface Items {
  id: number;
  name: string;
  description: string;
}
